name: Scheduled Run

on:
  schedule:
    # Runs at 11:54 UTC (5:24 PM IST) every day
    - cron: '54 11 * * *'

jobs:
  scheduled-task:
    runs-on: ubuntu-latest
    steps:
      - name: Nightly housekeeping
        run: echo "Running scheduled job at $(date -u)."
